package com.practicebank.online.inquiry;

import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * Phase 2: Lightweight unit test for inquiry-api.
 * Full integration test requires MyBatis wiring across modules — deferred to Phase 3.
 */
class InquiryControllerTest {

    @Test
    void contextPlaceholder_passes() {
        // Phase 2: placeholder — verifies test compilation and JUnit 5 platform.
        assertThat(true).isTrue();
    }
}
